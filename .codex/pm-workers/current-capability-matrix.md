## Golden Path Required Capabilities

Capability groups a worker or PM **must** hit to complete the 8-step Golden Path loop (GP steps: 1 register/login + create project → 2 register/bind PM + worker agents → 3 create session/orchestration → 4 dispatch task → 5 worker receives durable inbox item → 6 claim/complete → 7 PM review/accept → 8 reviewed result visible in Project Space). All 18 logical route groups are mounted in `backend/src/app.ts` as **19 `app.use(...)` calls across lines 145–163** (`app.use(authRoutes)` … `app.use(rewardPreviewRoutes)`). The project-space group spans two physical files since the Batch 14A split — `project-space.routes.ts` (GP-Required/Support) and `project-space-frozen.routes.ts` (Frozen), mounted on adjacent lines 149–150 — so the file count (19) is one higher than the logical-group count (18).

**Static verification (machine-checked, current):**
- Route files: `find backend/src/routes -maxdepth 1 -type f -name '*.routes.ts' | wc -l` → **19** (18 logical route groups; the 19th file is `project-space-frozen.routes.ts`, split out of the project-space group in Batch 14A)
- Backend test files: `find backend/tests -maxdepth 1 -type f -name '*.test.ts' | wc -l` → **31**

**Classification legend** (mirrors `docs/api-surface-freeze.md:11-16`):
- **Golden Path Required** — must-hit to complete the 8-step GP loop; keep, test, maintain.
- **Golden Path Support** — required to *operate* the loop; keep, test, maintain, **do not expand**.
- **Frozen / Exploratory** — built beyond the narrowed V1; no new endpoints, no removal.
- The 8 GP steps map to: auth+projects → step 1; agents → step 2; sessions → step 3; orchestrations → steps 4/6/7; agent-inbox → step 5; `project-space` files → step 8.

| # | Route file | Route / prefix | Capability | GP relation | Concrete evidence path(s) |
|---|---|---|---|---|---|
| 1 | `auth.routes.ts` | `POST /v1/auth/register`, `POST /v1/auth/token`, `GET /v1/auth/me` | User register / login / identity (JWT) | GP step 1 — user must register/login to create a project | `backend/tests/e2e-api.test.ts` (register + JWT), `backend/tests/auth-prefix-narrowing.test.ts`, `docs/api-surface-freeze.md:28` |
| 2 | `projects.routes.ts` | `/v1/projects` (CRUD), `/v1/projects/:id/summary`, `/v1/projects/:id/members` (CRUD) | Project + membership lifecycle + project-space summary | GP step 1 (Create Project) and step 8 (project scope for result visibility) | `backend/tests/e2e-api.test.ts` (project + member flows), `backend/tests/owner-agent.test.ts`, `backend/tests/agent-key-project-scope.test.ts`, `backend/tests/project-summary.test.ts`, `docs/api-surface-freeze.md:29` |
| 3 | `agents.routes.ts` | `/v1/projects/:pid/agents` (CRUD/send/runs), `/v1/agents/:aid` (get/patch/delete/health/retire), `/v1/agents/heartbeat`, `/v1/agents/metrics`, `/v1/users/me/agents` | Agent registration, dispatchable presence, health/metrics | GP step 2 — bring PM + worker agents online; heartbeat/metrics gate dispatchability | `backend/tests/agent-identity.test.ts`, `backend/tests/credential-lifecycle.test.ts`, `backend/tests/agent-key-project-scope.test.ts`, `backend/tests/e2e-api.test.ts` (agent + heartbeat), `docs/orchestration.md:53-73` |
| 4 | `sessions.routes.ts` | `/v1/projects/:pid/sessions` (CRUD, messages), `/v1/sessions/:sid` (messages) | Session + message creation | GP step 3 — create orchestration/session; session is the notification channel | `backend/tests/session-dispatch.test.ts`, `backend/tests/e2e-api.test.ts` (session + messages), `docs/orchestration.md:77-89` |
| 5 | `orchestrations.routes.ts` | `/v1/projects/:pid/orchestrations` (CRUD, tasks, claim, complete, review, main-agent switch) | Orchestration + task state machine | GP steps 4 (dispatch), 6 (claim/complete), 7 (PM review) | `backend/tests/orchestrations.test.ts`, `backend/tests/session-dispatch.test.ts`, `docs/orchestration.md:102-171` (dispatch/claim/complete/review API), `backend/README.md:87-94` |
| 6 | `agent-inbox.routes.ts` | `/v1/agent/inbox`, `/v1/agent/inbox/:id/ack`, `/v1/agent/workload`, `/v1/agent/projects`, `/v1/projects/:pid/workload` | Durable inbox + workload tracking | GP step 5 — worker receives durable inbox item; inbox is single source of truth for pending work | `backend/tests/agent-inbox.test.ts`, `backend/tests/inbox-reliability.test.ts`, `backend/tests/workload-p05.test.ts`, `docs/orchestration.md:214-246` |

## Golden Path Support Capabilities

Required to **operate** the loop (monitoring, metrics, user mgmt, real-time). Keep, test, maintain — **do not expand** (`docs/api-surface-freeze.md:14,35`; New Endpoint Gate Rule). These groups are mandatory for operations but do not advance the GP step counter themselves.

| # | Route file | Route / prefix | Capability | GP relation | Concrete evidence path(s) |
|---|---|---|---|---|---|
| 7 | `project-space.routes.ts` (GP portions) | `/v1/projects/:pid/files` (CRUD, upload, search, raw/download/blame, revisions/compare), `/v1/projects/:pid/readme`, `/v1/projects/:pid/agents-rules` (AGENTS.md), `/v1/projects/:pid/archive.zip`, `/v1/projects/:pid/memories` (CRUD), `/v1/projects/:pid/join-requests` (CRUD/review) | Reviewed result visibility, agent memory, V1 membership gateway | GP step 8 (file/memory visibility); join-requests = V1 project membership. Frozen-surface guard (`scripts/validate-project-space-routes.js`) enforces 27-route manifest. | `backend/tests/project-space.test.ts`, `backend/tests/owner-agent.test.ts`, `backend/tests/project-space-route-guard.test.ts`; `docs/api-surface-freeze.md:39,69-89` |
| 8 | `health.routes.ts` | `/v1/health`, `/v1/projects/:pid/health`, `/v1/projects/:pid/health/incidents` | Availability probe, project health, health-context incident list | Operations: readiness / availability probe | `backend/tests/e2e-api.test.ts` (health endpoint), `backend/README.md:103-110`, `docs/golden-path-ci-evidence.md:11-22` |
| 9 | `users.routes.ts` | `/v1/users/search`, `/v1/users/me/owner-agent` (get, patch) | User search + owner-agent binding | Support for agent→owner dispatch (GP step 2 binding) | `backend/tests/user-search.test.ts`, `backend/tests/owner-agent.test.ts`, `docs/api-surface-freeze.md:42` |
| 10 | `events.routes.ts` | `/v1/sessions/:sid/events`, `/v1/sessions/:sid/stream`, `/v1/projects/:pid/events` | SSE real-time streaming + webhook delivery | Real-time session comms + webhook delivery | `backend/tests/event-persistence.test.ts`, `backend/tests/webhook-security.test.ts`, `backend/tests/webhook-delivery.test.ts`, `backend/tests/e2e-api.test.ts` (SSE), `backend/README.md:96-101` |
| 11 | `notification-metrics.routes.ts` | `/v1/projects/:pid/notification-metrics`, `/v1/admin/notification-metrics` | TTFT north-star metric surface + cross-project view | TTFT measurement backing metric (Gate 8 reads this) | `backend/tests/notification-metrics.test.ts`, `docs/api-surface-freeze.md:43`, `docs/pre-release-gate-runbook.md:221-226` |
| 12 | `collaboration-requests.routes.ts` | `/v1/requests` (CRUD/approve/reject/cancel), `/v1/agent/request-owner-bind` | Agent-initiated owner binding + request workflow | Support for GP step 2 (owner bind); strict surface freeze | `backend/tests/collaboration-requests.test.ts`, `backend/tests/collaboration-request-migration-ddl.test.ts`, `backend/tests/agent-initiated-bind.test.ts`, `docs/api-surface-freeze.md:44` |

## Frozen / Exploratory Capabilities

Built beyond the narrowed V1; `golden-path-ledger.md` "Do Not Build Now" applies. No new endpoints; no removal (nonfunctional change only) (`docs/api-surface-freeze.md:15,46`). Routes are mounted and tested but must not grow.

| # | Frozen route / sub-route | Why frozen (GK "Do Not Build Now") | Evidence path(s) |
|---|---|---|---|
| 13 | `versioning.routes.ts` — `/v1/projects/:pid/branches`, `/commits`, `/changesets` (review/merge/rebase), `/rollback` | "Complex version-control semantics beyond one reviewed-change path." Branches/commits/changesets/merge/rebase/rollback are all beyond "one reviewed-change path." | `backend/tests/versioning.test.ts`, `docs/api-surface-freeze.md:50`, `docs/versioning-and-gates.md` |
| 14 | `incidents.routes.ts` — `/v1/incidents` (list/get/patch), `/v1/projects/:pid/agents/:aid/incidents`, `/health-check` | "A separate observability product around health/incidents." Platform-wide incident mgmt w/ status transitions. | `backend/tests/incident-rbac.test.ts`, `docs/api-surface-freeze.md:51` |
| 15 | `mcp.routes.ts` — `/v1/projects/:pid/mcp/capabilities` (CRUD) | "Protocol compatibility abstractions." MCP bridge capability registration. | `backend/tests/mcp.test.ts` (route-level: unauth denial, owner/admin register/delete, owner/member list, validation errors, wrong-project delete), `backend/package.json:8` (in unit-test chain), `docs/api-surface-freeze.md:52` |
| 16 | `debug.routes.ts` — `/v1/debug/logs`, `/v1/debug/logs/config` | Operator debug tooling, not in GP. Already gated behind `DEBUG_LOG_API_TOKEN`. | `backend/tests/debug-log-api.test.ts`, `backend/tests/logger-debug.test.ts`, `docs/api-surface-freeze.md:53` |
| 17 | `reward-preview.routes.ts` — `GET /v1/projects/:project_id/reward-preview`, `POST /v1/projects/:project_id/reward-preview/recalculate`, `POST /v1/work-units/:work_unit_id/adjust` | Reward/contribution calc; does not remove a real GP manual step. (Paths corrected from source: project-scoped reward-preview + recalculate + per-work-unit adjust, all owner/admin-gated.) | `backend/src/routes/reward-preview.routes.ts:132-311`, `backend/tests/reward-preview.test.ts`, `docs/api-surface-freeze.md:54` |
| 18 | `gates.routes.ts` — `/v1/gate-templates`, `/v1/projects/:pid/gates` (CRUD/attempts/prefilter/review) | "No marketplace, cross-org collaboration, or protocol abstractions." Admission machinery deferred past V1. | `backend/tests/gates.test.ts`, `docs/api-surface-freeze.md:55` |
| — | `project-space.routes.ts` frozen sub-routes: `/v1/projects/:pid/clone` | Edge operation, not in GP. | `docs/api-surface-freeze.md:72`, `backend/tests/project-space.test.ts` |
| — | `project-space.routes.ts` frozen sub-routes: `/v1/projects/:pid/file-proposals` (CRUD/review/approval) | Change-suggestion workflow; overlaps versioning; beyond "one reviewed-change path." | `backend/tests/file-proposals.test.ts`, `docs/api-surface-freeze.md:73` |

## Verified Proof / Evidence

**⚠ LOCAL PARITY ONLY.** All evidence below is local-runtime parity, not live CI, not production. The default local backend runs `NODE_ENV=test` (in-memory SQLite). **Exception (local proof, not live-CI/production):** PostgreSQL is proven on a *local ephemeral* PostgreSQL 16.14 run via the accepted Batch 13 transcript (`20260620_171530_c` + `20260620_173355_c`), and Node 22 build/test parity is proven locally (`20260620_170136_d`) — see the Batch 13 reconciliation block below and Gaps #2 / #4. No product code was changed to produce this matrix; it is a documentation/completeness artifact only.

**Repo-side static proof (current, machine-checked):**
- 19 Express route files present (18 logical route groups); `backend/src/app.ts:145-163` mounts all of them as 19 `app.use(...)` calls (`authRoutes` … `rewardPreviewRoutes`, with `projectSpaceFrozenRoutes` at line 150) — mount lines re-confirmed this turn.
- 31 backend test files present (`backend/tests/*.test.ts`).
- `backend/src/data-source.ts:105-112`: `isTestEnv` selects `better-sqlite3` + `:memory:`; the Postgres branch lives at `backend/src/data-source.ts:113-124` → **local parity does not require Postgres** (`docs/golden-path-ci-evidence.md:207`).
- reward-preview route paths read directly from source this turn (`backend/src/routes/reward-preview.routes.ts`): `GET /v1/projects/:project_id/reward-preview` (line 137), `POST /v1/projects/:project_id/reward-preview/recalculate` (line 202), `POST /v1/work-units/:work_unit_id/adjust` (line 251). All three require owner/admin membership.
- GP loop + durable-inbox + state machine documented: `docs/orchestration.md` (steps, API flow, orchestration states `planning→running→ready_for_acceptance→completed`, task states `pending→claimed→completed→reviewed`).
- Pre-release gate chain (8 gates, incl. TTFT) defined: `docs/pre-release-gate-runbook.md:10-22`.
- CI `golden-path-smoke` job structure defined: `docs/golden-path-ci-evidence.md:167-181` (checkout → setup Node 22 → install → build → start backend `NODE_ENV=test PORT=3000 INBOX_LEASE_TTL_MS=2000` → orchestration+lease smoke → measure TTFT → API E2E → stop).
- Local parity command exists and is durable: `scripts/golden-path-smoke.sh` mirrors the CI job's command sequence and start env; exits non-zero if smoke or E2E fails (`docs/golden-path-ci-evidence.md:189-205`).
- TTFT is enacted by a canonical script: `deploy/measure-ttft.sh` asserts `time_to_first_reviewed_task_ms` < `TTFT_THRESHOLD_MS` (default **300 000** ms); env reference at `docs/pre-release-gate-runbook.md:241-253`.
- Remote-opt-in guard preserved as a layered invariant: `deploy/verify.sh` `require_local_or_opt_in()` + `deploy/smoke.sh` (lines 151-161) both enforce localhost / `ALLOW_REMOTE_VERIFY=1` (`docs/pre-release-gate-runbook.md:197-209`).
- `backend/README.md` is the route inventory + per-route test map that backs this matrix's file→test citations (e.g. `backend/README.md:87-110`).

**⚠ Correction — do NOT cite `scripts/validate-pm-result-contract.js` as proof for this matrix's headers.**
- `scripts/validate-pm-result-contract.js` validates a **different** contract: a **six-section PM assessment report** that must open with `## Product Read` and contain exactly these six top-level sections, in order — `## Product Read`, `## Architecture Map`, `## Top 8 Recommendations`, `## Two-Week Roadmap`, `## Verification Performed`, `## Evidence Gaps And Residual Risks` — plus exactly 8 recommendation blocks (see `scripts/validate-pm-result-contract.js:32-39, 102-136`).
- This capability matrix uses a **different five-header shape** (`## Golden Path Required Capabilities` … `## Remaining Proof Gaps`). Running that validator on this matrix would **fail** (wrong first line, wrong count, wrong headers). The earlier matrix's claim that the validator made "the five required headers enforceable" was **false** and is withdrawn here. The validator proves the PM assessment contract only — never this five-section capability matrix.

**Prior accepted task `20260620_140722_e` (verdict `accept`, ATTACKED→SURVIVED) — LOCAL PARITY ONLY (not re-run this turn):**
- `bash scripts/golden-path-smoke.sh` exited 0; backend healthy in ~2s on `127.0.0.1:3000`; orchestration smoke passed (lease suppression + redelivery exercised); API E2E `21/21` passed (`backend/tests/e2e-api.test.ts`).
- Runtime delta: local Node v24 / npm 11.9 vs CI Node 22 — command/env parity, **not** binary parity.

**Batch 10 local evidence (accepted this turn):**
- Full local pre-release gates 1–8 accepted via task `20260620_151003_d` (verdict `accept`): `deploy/verify.sh` run end-to-end including typecheck + unit + dashboard + SDK/CLI import + TTFT measurement, all passed locally.
- TTFT measurement accepted via `20260620_151003_d`: `deploy/measure-ttft.sh` asserted `time_to_first_reviewed_task_ms` < 300 000 ms, passed.
- Dashboard E2E report `deploy/dashboard-e2e-artifacts/report_20260620T154649Z.json`: 19 passed / 0 failed.
- Onboarding smoke report `deploy/onboarding-smoke-artifacts/report_20260620T154728Z.json`: 11 passed / 0 failed.
- Artifact redaction / secret scan accepted via task `20260620_152816_c`: zero credential-like pattern matches across `deploy/dashboard-e2e-artifacts` and `deploy/onboarding-smoke-artifacts`.

**Out of scope this turn:** this is a formatting/completeness repair of the matrix only — no source code changed and no live smoke / CI / TTFT / Postgres run is claimed or re-executed. The hard proof gaps below remain open.

**Batch 12 local evidence (accepted this turn):**
- `backend/tests/webhook-delivery.test.ts` created and passed: delivers payload + HMAC signature to local receiver; on 500 makes 1 initial attempt + 4 retries (5 total) with observable backoff before dead-letter; recovers after transient failures (fail 2×, succeed on the 3rd attempt).
- Source change: `backend/src/services/webhook.service.ts` accepts `WEBHOOK_RETRY_DELAYS_MS` env var for test-time retry delay override (backward-compatible; production defaults unchanged), safe-parses it via `parseRetryDelays` (malformed → default), and masks userinfo/query (and URLs inside error messages) in failure logs via `maskUrl`/`maskMessage`.
- Build + all three webhook/event tests pass: `webhook-security.test.ts` (exit 0), `event-persistence.test.ts` (exit 0), `webhook-delivery.test.ts` (exit 0).
- Test added to `backend/package.json` `test:unit` chain after `webhook-security.test.js`.
- `backend/tests/workload-p05.test.ts` Test 10 added: 4 concurrent worker agents, 20 dispatched tasks, single in-memory SQLite backend process. Measures p95 poll/claim/complete and end-to-end task latencies against conservative local-only thresholds (p95 poll ≤ 2000 ms, claim ≤ 3000 ms, complete ≤ 3000 ms, e2e ≤ 6000 ms). Verified that every task produces a `task_ready_for_review` inbox item and that every delivered inbox item carries a lease token. Build + workload-p05 test pass (`npm run build` exit 0, `node dist/tests/workload-p05.test.js` exit 0). This evidence is local parity only and explicitly does **not** claim production p95 performance.

**Batch 12 PostgreSQL parity sharpening (hermes-c) — ⚠ SUPERSEDED: the "gap remains open / reproducibly BLOCKED" outcome below was superseded by the accepted Batch 13 local positive proof (see the Batch 13/14C reconciliation block). Retained for traceability; do not read the BLOCKED conclusion as current — the local Postgres gap is now closed as local proof (Gap #2).**
- Goal: close or precisely downgrade Remaining Proof Gap #2 (PostgreSQL). Outcome: **gap remains open but is now reproducibly blocked** — no live Postgres proof was possible because this machine has zero Postgres tooling (no docker/podman/colima/orbstack, no psql/pg_isready/pg_ctl/initdb, no brew postgres, no Postgres.app).
- No-DB partial proof that DOES pass: `node scripts/ci-migration-check.js` → **exit 0** ("18 migration(s) compiled and importable") — all 18 migrations, including their `driver === 'postgres'` branches, are valid importable TypeORM migrations.
- Live-DB blocker (reproducible): `cd backend && npm run migration:show` → **exit 1**, `AggregateError [ECONNREFUSED]` on `::1:5432` and `127.0.0.1:5432`; `nc -z 127.0.0.1 5432` → closed. The TypeORM postgres branch is correctly wired — it just has no server to reach.
- New reusable, secret-free parity script: `backend/scripts/pg-parity-check.sh` (= `npm run pg-parity`). It auto-provisions a throwaway `postgres:16` on docker/podman, or uses a caller-supplied local Postgres, runs `migration:show` + `migration:run` (+ optional Golden Path), and **fails closed (exit 2)** when no Postgres is available — run this turn → exit 2 BLOCKED.
- Files changed (in-scope only): `backend/scripts/pg-parity-check.sh` (new), `backend/package.json` (added `pg-parity` convenience script), `docs/golden-path-ci-evidence.md` (PostgreSQL parity section), this matrix (Gap #2 sharpened + this block). No production source code changed.

**Batch 14D local load proof sharpening (hermes-d, this turn):**
- Goal: close or precisely downgrade Remaining Proof Gap #5 (live production p95 / concurrency load). Outcome: **gap remains open but local evidence is now machine-readable and reproducible** — still in-memory SQLite, still not production Postgres/network load.
- Enhanced `backend/tests/workload-p05.test.ts` Test 10 to report **p50/p95/p99** latencies for poll/claim/complete/e2e paths (previously only p95), while keeping conservative local-only thresholds (p95 poll ≤ 2000 ms, claim ≤ 3000 ms, complete ≤ 3000 ms, e2e ≤ 6000 ms; p99 thresholds are looser and exist only to catch pathological runs). The test explicitly does **not** claim production p95 performance.
- The test now writes a structured JSON artifact when `WORKLOAD_ARTIFACT_PATH` is set. The artifact schema is `local-load-proof/v1` and includes environment, database type, worker/task counts, durations, thresholds, inbox guarantees, and a `note` that the evidence is local-only / not production parity.
- New repeatable script: `backend/scripts/load-proof-check.sh` (= `npm run load-proof`). Default mode builds the backend and runs the enhanced workload-p05 test against SQLite, writing an artifact to `backend/load-proof-artifacts/local-load-proof-<timestamp>.json`. Optional `--postgres` / `--container` / `--local` modes attempt the same load proof against a real PostgreSQL instance; if Postgres is unavailable the script prints a precise blocker and exits **2 (BLOCKED)**.
- Build + workload-p05 test pass; the new script passes in local SQLite mode and exits 2 BLOCKED in `--postgres` mode on this machine (no docker/podman/local Postgres). This evidence is local parity only.

**Batch 14E local credential rotation proof sharpening (hermes-e, this turn):**
- Goal: close or precisely downgrade Remaining Proof Gap #6 (credential rotation in production). Outcome: **gap remains open but local evidence now covers rotate, revoke, retire, and supersede key denial** — all exercised at the API level against a live local server. Production deployment rotation/revocation remains unproven.
- `backend/tests/credential-lifecycle.test.ts` now has **48 assertions** covering the agent credential lifecycle (rotate, revoke, retire, supersede):
  - **Rotate key** (13 checks): key creation, original key auth (profile + heartbeat), rotation (200, new key starts with `zzk_`, new key differs, old key returns 401 for both profile and heartbeat), non-member deny (403), agent-key-self-rotate deny (401).
  - **Revoke key** (11 checks): revocation (200, nullifies `api_key_prefix`), revoked key returns 401 for both profile and heartbeat, non-member deny (403), agent-key-self-revoke deny (401), re-activation via rotate after revoke works (200).
  - **Retire agent** (8 checks): project-scoped retire sets lifecycle_status to `retired`, status to `inactive`, retired_at timestamp, preserves api_key_prefix; retired key denied (401 — auth middleware filters `INACTIVE` agents at `auth.ts:160`); non-member deny (403); double-retire returns 409.
  - **Supersede agent** (13 checks via root V1 retire endpoint): creates replacement agent, supersedes original with `superseded_by`, sets lifecycle_status to `superseded`, **sets status to `inactive`**, sets `superseded_by_agent_id`, retired_at; agent list reflects superseded lifecycle and superseder reference; supersede with inactive replacement returns 409; **superseded agent's old key returns 401 for both profile and heartbeat**.
- Permission model verified: non-members receive 403 for all lifecycle operations; agent API keys cannot rotate/revoke/retire themselves (401); only authenticated JWT users with EditAgent permission can perform these operations.
- Auth middleware behavior verified: `rotate-key` nullifies the old bcrypt hash so old key is denied; `revoke-key` nullifies `apiKeyHash` so revoked key is denied; `retire` does NOT nullify the key but the middleware (`auth.ts:160`) filters `status: INACTIVE` agents, so retired keys are also denied. Root retire/supersede now additionally sets `status: INACTIVE`. As defense-in-depth, API-key auth also filters `lifecycle_status: retired` and `superseded` (`auth.ts:156`), so lifecycle-terminated keys are denied even if a status field were left active. Only the legitimate owner JWT can re-activate a revoked agent via rotate-key.
- Build + credential-lifecycle test pass: `npm run build` exit 0, `node dist/tests/credential-lifecycle.test.js` → 48 passed, 0 failed. `node dist/tests/agent-key-project-scope.test.js` → 39 passed, 0 failed.
- Token/API keys are never printed in full in test output or evidence artifacts — only prefix (`zzk_` prefix assertion) and redacted references are used. The test receives the full key from the API response and uses it only for follow-up API calls, never logging it.
- **Residual production caveat**: all evidence is local parity only (in-memory SQLite, single process, no TLS/mTLS, no key rotation schedule or policy). A live production rotation would additionally need to verify: key roll-out sequencing (dual-key window during rotation), revocation propagation delay across load-balanced instances, TLS-level key pinning or mTLS certificate rotation, and a rotation schedule/policy (e.g. key expiry, automatic rotation). These are infrastructure/deployment concerns beyond what local API-level testing can prove.

**Batch 14A project-space route risk closure (hermes-a, this turn):**
- Goal: close Remaining Proof Gap #3 (mixed `project-space.routes.ts` route-file risk). Outcome: **gap closed** — frozen sub-routes physically separated into a dedicated file; guard updated to check both files.
- Physical separation: frozen routes (clone, file-proposals) extracted from `project-space.routes.ts` into `project-space-frozen.routes.ts`. Shared helper functions extracted to `project-space.utils.ts`. The remaining `project-space.routes.ts` contains only GP-Required (files CRUD + revisions) and GP-Support (memories CRUD, join-requests).
- `backend/src/app.ts` updated: new import `projectSpaceFrozenRoutes` added (line 12); `app.use(projectSpaceFrozenRoutes)` mounted after `projectSpaceRoutes` (line 151). All 18 route groups still mounted; route count unchanged.
- Guard script updated: `scripts/validate-project-space-routes.js` now parses **both** source files (`VALIDATE_PS_SOURCE` + `VALIDATE_PS_FROZEN_SOURCE` env overrides), combines parsed routes, and compares against the same 27-route manifest. Error messages updated to reference "project-space route files" instead of a single file.
- Guard test updated: `backend/tests/project-space-route-guard.test.ts` assertions now verify both source files are mentioned in guard output.
- Documentation updated: `docs/api-surface-freeze.md` §Automated Frozen-Surface Guard updated to reflect two-file structure; PM Action #1 marked done.
- Capability matrix updated: Gap #3 status changed from "now guarded (partially mitigated)" to "closed (Batch 14A)"; evidence paths updated to include all three new/modified files.
- **No endpoint behavior, auth semantics, or API paths changed.** All routes serve the same paths with the same handlers.
- Verification: `cd backend && npm run build` → exit 0; `node dist/tests/project-space-route-guard.test.js` → exit 0 (3/3 passed); `node scripts/validate-capability-matrix.js .codex/pm-workers/current-capability-matrix.md` → exit 0.
- **Residual risk:** none for the route-file mixing concern. The guard still only matches literal `router.METHOD('...')` registrations (documented limitation), but both files use only literal registrations.

**Batch 13 accepted-evidence reconciliation (hermes-c, Batch 14C) — reconciles Gaps #1 / #2 / #4 below to the accepted ledger state:**

These three tasks were accepted in the golden-path ledger (`accepted_*` verdicts). No proof was re-run this turn — this block records what the accepted evidence closed (and what residual risk remains). Local proof is strictly distinguished from live-CI / production proof throughout.

- **Node 22 / reproducible-build parity — closed as LOCAL proof** (accepted `20260620_170136_d`, verdict `accept`, `ATTACKED→SURVIVED`). A temporary Node 22.14.0 binary (npm 10.9.2), downloaded into the task ledger dir with **no global/persistent changes**, ran the backend chain under Node 22: `cd backend && npm ci` → exit 0 (255 packages); `npm run build` → exit 0 (Gate 1 typecheck passes); `npm test` → exit 0 (all suites passed). → Reconciles Gap #4 to "local Node 22 build+test parity proven". **Residual (preserved):** the Node 22 binary is local to the task dir only — **not** the default session (default stays v24.14.0); proof is **local macOS only**; a fresh `npm ci`+build+test on Linux / live-CI `ubuntu-latest` was **not** reproduced, and the live-CI Node 22 run remains unobserved (blocked).

- **Live CI — still BLOCKED** (accepted `20260620_170835_d`, verdict `accept`, `ATTACKED→SURVIVED`; plus Batch 14B runbook). The live `golden-path-smoke` run is still **❌ Not observed**. Precise, reproducible preconditions confirmed: `pwd` → exit 0; `git status --short` → exit 128 (`fatal: not a git repository`); `git rev-parse --show-toplevel` → exit 128; `gh auth status` → exit 1; `gh repo view --json ...` → exit 4. The refined repo candidate is **`zhuzeyang/zz-agent`** (read from `sdk/python/pyproject.toml` `Source` field), but it remains unvalidated until `gh` is authenticated and the slug is confirmed. `.github/workflows/ci.yml` is 172 lines (corrected this turn). → Gap #1 stays open / blocked; needs `gh auth login` (or `GH_TOKEN`) + explicit `--repo OWNER/REPO`. No GitHub Actions evidence is present, so Live CI must remain blocked.

- **PostgreSQL — closed as LOCAL proof** (accepted `20260620_171530_c` transcript + repair `20260620_173355_c`, verdict `accept`, `ATTACKED→SURVIVED`). The full sanitized 1053-line transcript is the acceptance artifact at `.codex/pm-workers/tasks/20260620_171530_c/result.md`. Scope: **PostgreSQL 16.14 on this host, one ephemeral run** (Homebrew cached bottle → temp PGDATA in `/tmp`, scram-sha-256, superuser `pgparity`, `127.0.0.1:55432`; throwaway creds destroyed at teardown). What passed against real PostgreSQL 16.14: `npm run build` exit 0; `migration:show` → 18 pending, exit 0; `migration:run` → **all 18 applied** (exit 0); DB-side 18/18 migration rows + 18 enum types + 30 user-entity tables, `users` columns/FKs verified; **adversarial** — correct cred connects (`ok`), WRONG cred rejected (`FATAL: password authentication failed`, psql rc 2 = server genuinely validated); **Golden Path e2e against the Postgres-backed backend → 21 passed / 0 failed**, exit 0; cleanup `pg_ctl stop` rc 0, `brew uninstall postgresql@16` rc 0, autoremove rc 0. → Reconciles Gap #2 from "BLOCKED/ECONNREFUSED, exit 2" to "local positive proof exists". **Residual (preserved, not erased):** local proof only — **not** live CI (`ubuntu-latest`) and **not** production; single host + single PG minor (16.14); reproducible-on-demand not continuous — Postgres tooling is **uninstalled post-cleanup**, so `bash backend/scripts/pg-parity-check.sh` **currently exits 2 (BLOCKED)** until re-provisioned (brew + cached bottle or network); benign Homebrew cache (~18.2 MB) + post-install log remain; "full machine restoration" explicitly not claimed.

- **Scope of this turn:** documentation reconciliation only — no source code changed, no Postgres / live-CI / Node proof re-run. `docs/golden-path-ci-evidence.md` (Live CI runbook already expanded by Batch 14B; PostgreSQL Parity and Node 22 parity sections reconciled here) and this matrix (Gaps #1/#2/#4 + caveat + this block) are the changed artifacts.

## Remaining Proof Gaps

| # | Gap | What is missing / not cited | Why it remains a gap | Evidence / command path |
|---|---|---|---|---|
| 1 | **Live CI** | No live `golden-path-smoke` run observed; remains **❌ Not observed / BLOCKED**. Precise preconditions confirmed by accepted `20260620_170835_d` (verdict `accept`): `pwd` → exit 0; `git status --short` → exit 128 (`fatal: not a git repository`); `git rev-parse --show-toplevel` → exit 128; `gh auth status` → exit 1; `gh repo view --json ...` → exit 4; no `GH_TOKEN`/`GITHUB_TOKEN`. Refined repo candidate **`zhuzeyang/zz-agent`** (from `sdk/python/pyproject.toml` `Source`), unvalidated until `gh` is authenticated; `.github/workflows/ci.yml` is 172 lines. | Cannot cite a passing GitHub Actions run. Dual precondition gap: no `.git` checkout **and** no `gh` auth. Must stay blocked until a human with credentials runs the Batch 14B runbook (`gh auth login` or `GH_TOKEN` + explicit `--repo zhuzeyang/zz-agent`, validated slug). | `docs/golden-path-ci-evidence.md:11-165` (Live CI Observation: status, probe table, runbook + refusal conditions); `sdk/python/pyproject.toml` (`Source` repo candidate) |
| 2 | **PostgreSQL — closed as LOCAL proof (accepted Batch 13); live-CI/production residual** | `NODE_ENV=test` defaults to in-memory SQLite (`better-sqlite3` `:memory:`). **Local positive proof now exists and is accepted** (`20260620_171530_c` transcript + `20260620_173355_c` repair, verdict `accept`/`SURVIVED`): on an **ephemeral PostgreSQL 16.14** run (Homebrew cached bottle → temp PGDATA in `/tmp`, scram-sha-256, `127.0.0.1:55432`, throwaway creds destroyed at teardown) — `npm run build` exit 0; `migration:show` → 18 pending (exit 0); `migration:run` → **all 18 applied** (exit 0); DB-side 18/18 migration rows + 18 enum types + 30 user-entity tables, `users` verified; **adversarial** correct-cred connects / wrong-cred rejected (`FATAL`, rc 2); **Golden Path e2e against the Postgres-backed backend → 21 passed / 0 failed**. No-DB partial proof still holds: `node scripts/ci-migration-check.js` → exit 0. The reusable secret-free check `bash backend/scripts/pg-parity-check.sh` (= `npm run pg-parity`) reproduces it on a Postgres-capable machine; `--local` preserves caller `DB_*`; it **fails closed (exit 2)** when no Postgres is present. | Gap is **closed as local proof** (migrations applied + GP-vs-Postgres exercised on this host against PG 16.14). **Residual risk is preserved, not erased:** (a) local proof only — **not** live CI (`ubuntu-latest`) and **not** production; (b) single host + single PG minor (16.14); (c) reproducible-on-demand, not continuous — Postgres tooling is **uninstalled post-cleanup**, so `pg-parity` **currently exits 2 (BLOCKED)** until re-provisioned (brew + cached bottle or network); benign Homebrew cache (~18.2 MB) + post-install log remain, "full restoration" not claimed. | `.codex/pm-workers/tasks/20260620_171530_c/result.md` (full sanitized transcript, the acceptance artifact); `backend/src/data-source.ts:105-124`; `backend/scripts/pg-parity-check.sh`; `backend/package.json` (`pg-parity`); `backend/scripts/ci-migration-check.js`; `docs/golden-path-ci-evidence.md:209-257` (PostgreSQL parity section); `docs/deployment.md` (CI `postgres:16` service container intent, still unobserved) |
| 3 | **Mixed `project-space.routes.ts` route-file risk — closed (Batch 14A; manifest reconciled Batch83)** | Frozen sub-routes (clone, file-proposals) are now physically separated into `project-space-frozen.routes.ts`. The remaining `project-space.routes.ts` contains GP-Required (files CRUD, search, raw/download/blame, revisions/compare) and GP-Support (memories, join-requests). A machine-checkable guard now exists: `scripts/validate-project-space-routes.js` parses **both** source files and compares against a 27-route manifest (17 GP-Required, 5 GP-Support, 5 Frozen), AND cross-checks the same family counts + frozen sub-route names against `docs/api-surface-freeze.md` and this matrix, so a developer cannot update the manifest/source but forget the docs/matrix. Test `project-space-route-guard.test.ts` proves both the passing case and drift rejection (doctored freeze-doc/matrix temp files exit non-zero) and runs as part of `npm test`. | Gap **closed**: physical file separation reduces accidental expansion risk; guard machine-enforces source + manifest + freeze docs + matrix agreement across both files. Shared utilities extracted to `project-space.utils.ts`. The source parser matches literal `router.METHOD('...')` registrations only. | `scripts/validate-project-space-routes.js`; `backend/tests/project-space-route-guard.test.ts`; `docs/api-surface-freeze.md` (§3 + Automated Frozen-Surface Guard section); `backend/src/routes/project-space.routes.ts`; `backend/src/routes/project-space-frozen.routes.ts`; `backend/src/routes/project-space.utils.ts` |
| 4 | **Node / reproducible-build parity — closed as LOCAL proof (Node 22); Linux/live-CI residual** | Default local Node is v24.14.0 / npm 11.9.0 vs CI Node 22 on `ubuntu-latest`. **Local Node 22 parity now proven and accepted** (`20260620_170136_d`, verdict `accept`/`SURVIVED`): a temporary Node 22.14.0 binary (npm 10.9.2, downloaded into the task dir — no global/persistent change) ran `cd backend && npm ci` → exit 0 (255 packages), `npm run build` → exit 0 (Gate 1 typecheck), `npm test` → exit 0 (all suites). | Gap **closed as local Node 22 build+test parity** on this macOS host. **Residual (preserved):** the Node 22 binary is task-dir-local only (default session stays v24.14.0); proof is **local macOS only** — a fresh `npm ci`+build+test on Linux and the live-CI `ubuntu-latest` Node 22 run were **not** reproduced and remain unobserved (Live CI blocked — see Gap #1). Command/env parity, not binary-identical cross-OS parity. | `docs/golden-path-ci-evidence.md:259-272` (Platform Differences table + Node 22 local parity / residual note, reconciled Batch 14C); `.codex/pm-workers/tasks/20260620_170136_d/result.md` (Node 22 proof) |
| 5 | **Live production p95 / concurrency load** | **Local proof:** `backend/tests/workload-p05.test.ts` Test 10 now provides **local** multi-agent concurrency evidence (4 concurrent workers, 20 tasks, **p50/p95/p99** poll/claim/complete/e2e latencies) against one in-memory SQLite backend process. `backend/tests/inbox-reliability.test.ts` continues to exercise inbox lease suppression/redelivery. A repeatable script, `backend/scripts/load-proof-check.sh` (= `npm run load-proof`), runs the test, enforces conservative local-only thresholds, and writes a `local-load-proof/v1` JSON artifact. An optional `--postgres` mode exists but is **blocked** on this machine (no docker/podman/local Postgres), exiting 2 rather than claiming production parity. **Live/staging proof entrypoint:** `backend/scripts/live-load-proof.sh` (= `npm run live-load-proof`) targets a real deployed base URL supplied by the operator via `BASE_URL`. It validates required env vars, enforces the existing `ALLOW_REMOTE_VERIFY=1` remote opt-in guard, runs a bounded Golden-Path workload, measures p50/p95/p99 latencies, and writes a redacted `live-load-proof/v1` JSON artifact. It exits **2 (BLOCKED)** when env vars are missing or opt-in is not granted. Secrets are read from env only and never printed or persisted. | This is **local parity only** unless the operator explicitly supplies and exercises a real deployed/staging target. The local SQLite proof must not be cited as a production p95 guarantee. The live entrypoint is safe by default (fail-closed, bounded traffic, no secrets in files), but a production claim still requires the operator to run it against the production environment during representative traffic. | `backend/tests/workload-p05.test.ts` (Test 10), `backend/tests/inbox-reliability.test.ts`, `backend/scripts/load-proof-check.sh`, `backend/scripts/live-load-proof.sh`, `backend/tests/live-load-proof.test.ts`, `docs/orchestration.md` (Concurrency and lease behavior) |
| 6 | **Credential rotation in production** | `backend/tests/credential-lifecycle.test.ts` now has **48 assertions** covering the agent credential lifecycle: rotate-key (13 checks), revoke-key (11), retire (8), supersede (13), and re-activation. See Batch 14E evidence block for the full assertion breakdown. `agent-key-project-scope.test.ts` (39 assertions) covers project-scoped key authorization (ALLOW/DENY matrix). **No production rotation/revocation run is cited.** | Key lifecycle proven at the API level with local in-memory SQLite, not against a live deployment. Production concerns — dual-key window, revocation propagation across load-balanced instances, TLS/mTLS pinning, rotation schedule — remain unproven. | `backend/tests/credential-lifecycle.test.ts` (48 assertions, pass); `backend/tests/agent-key-project-scope.test.ts` (39 assertions, pass); `backend/src/middleware/auth.ts:126-217` (API key auth with INACTIVE and lifecycle_status filtering); `backend/src/routes/agents.routes.ts:504-581` (rotate-key/revoke-key handlers), `backend/src/routes/agents.routes.ts:472-499,1099-1143` (retire handlers); `docs/orchestration.md:53-73` |
| 7 | **Webhook delivery reliability — resolved** | `backend/tests/webhook-delivery.test.ts` proves delivery success (local receiver gets payload + HMAC-SHA256 `X-ZZ-Signature` header), failure retry (500 → 1 initial attempt + 4 retries = 5 total attempts → dead letter), and transient-failure recovery (fail 2×, then succeed on the 3rd attempt = 1 initial + 2 retries). Attempt counts are named explicitly as "initial + retries" to avoid off-by-one ambiguity. Retry delays tested at 10/20/50/100ms via `WEBHOOK_RETRY_DELAYS_MS`; the test waits for each retry chain to settle (dead-letter or recovery) before the next test so prior logs cannot bleed. Failure/dead-letter logs mask userinfo as `***@` and query as `?***` (including URLs echoed inside error messages), so credentials/query tokens are never logged. `WEBHOOK_RETRY_DELAYS_MS` is safe-parsed — malformed entries (non-numeric, negative, empty) fall back to the default schedule rather than silently producing surprising retry timing. | Gap closed: all three delivery modes (success, fail-and-retry, fail-then-recover) are proven against local receivers with observable backoff timing; log masking and retry-delay validation are unit-checked. | `backend/tests/webhook-delivery.test.ts`; `backend/src/services/webhook.service.ts` (`parseRetryDelays`, `maskUrl`, `maskMessage`) |
| 8 | **Backend typecheck run and passed this turn** | Gate 1 (`cd backend && npm run typecheck`, `tsc --noEmit`) executed as part of `npm run build` during Batch 12 workload verification and exited 0. | Type safety re-asserted this turn; does not prove runtime behavior or close deeper gaps (live CI / Postgres / production load). | `docs/pre-release-gate-runbook.md:40-46`; `cd backend && npm run typecheck` |
| 9 | **Stale historical citation: task `20260620_142155_c`** | `.codex/pm-workers/tasks/20260620_142155_c/result.md` no longer matches its old review's recorded 66-line evidence — the file is now **92 lines** (`wc -l` confirmed this turn) | The review's recorded evidence length has drifted from the file; the citation is not a stable, reproducible anchor and must not be trusted as evidence for any capability claim | `.codex/pm-workers/tasks/20260620_142155_c/result.md` (now 92 lines, was recorded as 66); re-derive any needed evidence from current source instead |
| 10 | **Matrix five-header machine-check — now guarded (no longer an open gap)** | A reusable machine-check now exists and passes this turn: run `node scripts/validate-capability-matrix.js .codex/pm-workers/current-capability-matrix.md`. It enforces the exact opening line, exactly five top-level headings in order (literal match — trailing whitespace on any required heading is rejected), ≥90 lines, no `## Final Summary` wrapper, and the four required correction markers. | The guard is **structural only** — it proves the five-section shape and prevents header/shape drift, but it does **not** prove semantic correctness, route existence, test freshness, or any runtime evidence (live CI / TTFT / Postgres / load). Those deeper proof gaps remain open as listed above. | `scripts/validate-capability-matrix.js`; re-run: `node scripts/validate-capability-matrix.js .codex/pm-workers/current-capability-matrix.md` |
